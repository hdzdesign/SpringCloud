#APPLICATION NAME
spring:
  application:
    name: gestion-facturacion

#CONFIGURACIÓN CONFIG SERVER.
  cloud:
    config:
      fail-fast: true
      enable: true
      uri: http://user:password@localhost:8888


